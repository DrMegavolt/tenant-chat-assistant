# PLACEHOLDERS ONLY. Copy under .local/k8s/ and replace the endpoint and model.
# example.invalid is an IANA-reserved documentation domain, not a live provider.
baseUrl=https://llm.example.invalid/v1
model=replace-with-provider-model
timeoutSeconds=120
